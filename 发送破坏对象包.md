
```squirrel title:
if (!parentObj) {
	if (obj.isMyControlObject()) {
		//发送摧毁包被动对象；销毁obj特效
		sq_SendDestroyPacketPassiveObject(obj);
		return;
	}
}
```

```squirrel title:
obj.sendDestroyPacket(true); //对象类 发送破坏包
```
