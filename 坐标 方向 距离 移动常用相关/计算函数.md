
$$
abs函数
$$
#运算得到的结果无论如何都会得到正数结果


```squirrel title:
sq_Abs(600 - 900);
```


$$
Atan2三角函数
$$

#长度高度算出靠近高度的弧度
```squirrel title:
local atan = sq_Atan2(x.tofloat(), z.tofloat());
```


$$
Degree函数
$$
#将弧度转为角度
```squirrel title:
local angle = sq_ToDegree(atan);
```



$$
Radian函数
$$
#角度转为弧度
```squirrel title:
sq_ToRadian(angle);
```


$$
Cos函数
$$
#给角度值计算出长度距离 #一般用于圆形的长度运算

```squirrel title:
sq_Cos(angle);
sq_CosTable(angle);
```


$$
Sin函数 
$$
#给角度值计算出高度比率 #一般用于弧形的高度

```squirrel title:
sq_Sin(angle);
sq_SinTable(angle);
```