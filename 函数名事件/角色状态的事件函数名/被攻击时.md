```squirrel title:复仇者函数
function onDamage_fastmove(obj, attacker, boundingBox) {
	if (!obj) return;

}
```

```squirrel title:其他职业函数
function onDamage_FireHurricane(obj, attacker, boundingBox) {
	if (!obj) return;
}
```