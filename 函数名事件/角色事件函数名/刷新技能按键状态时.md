```squirrel title:这里可以设置技能亮不亮
function flushCommandEnable_Avenger(obj) {
	if (!obj) return S_FLOW_NORMAL;
	if (!obj.isInBattle()) {
		sq_SetAllCommandEnable(obj, false);
		return S_FLOW_RETURN;
	}
	if (isAvengerAwakenning(obj) == true) {
		if (obj.isMyControlObject()) 
		sq_SetAllCommandEnable(obj, false);
	}
}
```
